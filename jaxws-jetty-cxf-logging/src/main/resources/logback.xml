<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!-- PROPERTIES -->
    <property name="logFile" value="jaxws-jetty-cxf-logging.log" />
    <property name="logFile-ws" value="jaxws-jetty-cxf-logging-ws.log" />

    <!-- LOGBACK APPENDERS -->
    <appender name="APP_LOG_FILE"
        class="ch.qos.logback.core.rolling.RollingFileAppender">

        <File>${logFile}</File>

        <rollingPolicy
            class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logFile}.%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread][%logger{0}]
                %m%n
            </pattern>
        </encoder>
    </appender>

    <appender name="WS_LOG_FILE"
        class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${logFile-ws}</file>

        <rollingPolicy
            class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logFile-ws}.%d{yyyy-MM-dd}.log
            </fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread][%logger{0}]
                %m%n
            </pattern>
        </encoder>
    </appender>

    <logger name="info.source4code.soap.http.cxf" level="INFO" />

    <logger name="org.springframework" level="WARN" />
    <logger name="org.apache.cxf" level="WARN" />

    <!-- INFO level needed to log SOAP messages -->
    <logger name="org.apache.cxf.services" level="INFO" />

    <!-- APPLICATION LOG LEVEL -->
    <root level="WARN">
        <appender-ref ref="APP_LOG_FILE" />
    </root>

    <!-- SERVICE CALL LOG -->
    <logger name="org.apache.cxf.services" additivity="false">
        <appender-ref ref="WS_LOG_FILE" />
    </logger>

</configuration>